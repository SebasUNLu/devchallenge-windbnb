export enum FinlandLocations {
  Helsinki = "Helsinki, Finland",
  Turku = "Turku, Finland",
  Oulu = "Oulu, Finland",
  Vaasa = "Vaasa. Finland",
}

// const keys = Object.keys(FinlandLocations) as Array<keyof typeof FinlandLocations>

// console.log(keys)

// keys.map((key)=>{
//   console.log(FinlandLocations[key])
// })